<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <h1>All Languages (<%= @kinds.length %>)</h1>
    <br>
    <% @kinds.each do |kind| %>
    <%= link_to kind.language, kind_path(kind) %>
    (<%= kind.snippets.length %> Snippets)
    <hr>
    <% end %>
  </div>
</div>
